// useAuth.js — re-export for cleaner imports
// Usage: import { useAuth } from "../hooks/useAuth"
// instead of importing directly from context every time

export { useAuth } from "../context/AuthContext";